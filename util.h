#ifndef SCHEDULER_UTIL_H
#define SCHEDULER_UTIL_H

/* Returns the greatest common divisor of two ints */
int gcd(int a, int b);

/* Returns the lowest common multiple of an array with n values */
int lcm(int input[], int n);

#endif //SCHEDULER_UTIL_H
